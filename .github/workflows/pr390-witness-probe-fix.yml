name: PR390 witness probe fix

on:
  push:
    branches:
      - ops/pr390-witness-probe-fix-20260908

permissions:
  contents: write

jobs:
  patch:
    runs-on: macos-15
    env:
      EXPECTED_HEAD: a46cb63827c7268391ceb4093ce00f478e4f58fb
      TARGET_BRANCH: feat/mrl-0801-hf-acquisition-executor-20260908
    steps:
      - name: Checkout operations branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          path: ops

      - name: Checkout exact target branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ env.TARGET_BRANCH }}
          path: work
          fetch-depth: 0

      - name: Guard exact target head
        shell: bash
        run: |
          set -euo pipefail
          test "$(git -C work rev-parse HEAD)" = "$EXPECTED_HEAD"
          test "$(git -C work rev-parse HEAD^{tree})" = "a5d4b7b22983def5f03d226685be415f30357f45"

      - name: Apply deterministic fix
        shell: bash
        run: |
          set -euo pipefail
          python3 ops/.github/pr390_witness_probe_patcher.py work
          python3 -m py_compile \
            work/src/medscale/mesc/_mrl_0801_hf_acquisition_v1.py \
            work/scripts/mesc_mrl_0801_hf_acquire.py
          git -C work diff --check
          actual_paths="$(git -C work diff --name-only | sort)"
          expected_paths="$(printf '%s\n' \
            'scripts/mesc_mrl_0801_hf_acquire.py' \
            'specs/mesc-experiment-0/mrl-0801-hf-acquisition-provenance-v1.md' \
            'src/medscale/mesc/_mrl_0801_hf_acquisition_v1.py')"
          test "$actual_paths" = "$expected_paths"

      - name: Guarded fast-forward push
        shell: bash
        run: |
          set -euo pipefail
          git -C work config user.name "TheHalfMoon"
          git -C work config user.email "285091250+TheHalfMoon@users.noreply.github.com"
          git -C work add \
            scripts/mesc_mrl_0801_hf_acquire.py \
            specs/mesc-experiment-0/mrl-0801-hf-acquisition-provenance-v1.md \
            src/medscale/mesc/_mrl_0801_hf_acquisition_v1.py
          git -C work commit -m "fix(mesc): retain atomic publication witnesses"
          git -C work fetch origin "$TARGET_BRANCH"
          test "$(git -C work rev-parse FETCH_HEAD)" = "$EXPECTED_HEAD"
          git -C work push origin "HEAD:refs/heads/$TARGET_BRANCH"
