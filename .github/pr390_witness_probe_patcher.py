from __future__ import annotations

import sys
from pathlib import Path

root = Path(sys.argv[1])


def replace(path: str, old: str, new: str) -> None:
    target = root / path
    text = target.read_text(encoding="utf-8")
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"expected one replacement in {path}, found {count}")
    target.write_text(text.replace(old, new), encoding="utf-8")

replace(
    "src/medscale/mesc/_mrl_0801_hf_acquisition_v1.py",
    '''def _probe_atomic_descriptor_publication(*, root_fd: int) -> None:\n    """Prove unnamed-file atomic publication on this filesystem before network access."""\n    probe_dir_name = f".mrl-0801-publication-probe-{secrets.token_hex(16)}"\n    probe_fd: int | None = None\n    source_fd: int | None = None\n    created_dir = False\n    published = False\n    try:\n        os.mkdir(probe_dir_name, 0o700, dir_fd=root_fd)\n        created_dir = True\n        probe_fd = os.open(\n            probe_dir_name,\n            os.O_RDONLY | _O_DIRECTORY | _O_NOFOLLOW | _O_CLOEXEC,\n            dir_fd=root_fd,\n        )\n        probe_directory = os.fstat(probe_fd)\n        if not stat.S_ISDIR(probe_directory.st_mode):\n            raise MRL0801HfAcquisitionError(\n                "atomic publication capability probe did not bind a directory"\n            )\n        source_fd = _open_unnamed_temp_file(root_fd=probe_fd)\n        source = os.fstat(source_fd)\n        if not stat.S_ISREG(source.st_mode):\n            raise MRL0801HfAcquisitionError(\n                "atomic publication capability probe did not create a regular file"\n            )\n        _publish_open_descriptor_no_replace(\n            source_fd=source_fd,\n            root_fd=probe_fd,\n            target_name="probe",\n        )\n        published = True\n        linked = _descriptor_entry_stat(root_fd=probe_fd, name="probe")\n        if (\n            linked is None\n            or not stat.S_ISREG(linked.st_mode)\n            or _stat_descriptor_identity(linked) != _stat_descriptor_identity(source)\n        ):\n            raise MRL0801HfAcquisitionError(\n                "atomic publication capability probe produced an invalid identity"\n            )\n        os.unlink("probe", dir_fd=probe_fd)\n        published = False\n        os.close(source_fd)\n        source_fd = None\n        os.close(probe_fd)\n        probe_fd = None\n        os.rmdir(probe_dir_name, dir_fd=root_fd)\n        created_dir = False\n    except MRL0801HfAcquisitionError:\n        raise\n    except OSError:\n        raise MRL0801HfAcquisitionError(\n            "atomic descriptor publication capability probe failed safely"\n        ) from None\n    finally:\n        if source_fd is not None:\n            with contextlib.suppress(OSError):\n                os.close(source_fd)\n        if probe_fd is not None:\n            if published:\n                with contextlib.suppress(OSError):\n                    os.unlink("probe", dir_fd=probe_fd)\n            with contextlib.suppress(OSError):\n                os.close(probe_fd)\n        if created_dir:\n            with contextlib.suppress(OSError):\n                os.rmdir(probe_dir_name, dir_fd=root_fd)\n''',
    '''def _probe_atomic_descriptor_publication(*, root_fd: int) -> None:\n    """Prove atomic publication on the same filesystem without deleting probe namespace."""\n    parent_fd: int | None = None\n    source_fd: int | None = None\n    try:\n        parent_fd = os.open(\n            "..",\n            os.O_RDONLY | _O_DIRECTORY | _O_NOFOLLOW | _O_CLOEXEC,\n            dir_fd=root_fd,\n        )\n        root_observation = os.fstat(root_fd)\n        parent_observation = os.fstat(parent_fd)\n        if not stat.S_ISDIR(parent_observation.st_mode):\n            raise MRL0801HfAcquisitionError(\n                "atomic publication witness parent is not a directory"\n            )\n        if parent_observation.st_dev != root_observation.st_dev:\n            raise MRL0801HfAcquisitionError(\n                "safe same-filesystem atomic publication witness is unavailable"\n            )\n        source_fd = _open_unnamed_temp_file(root_fd=parent_fd)\n        source = os.fstat(source_fd)\n        if not stat.S_ISREG(source.st_mode):\n            raise MRL0801HfAcquisitionError(\n                "atomic publication witness did not create a regular file"\n            )\n        witness_name = f".mrl-0801-publication-witness-{secrets.token_hex(16)}"\n        _publish_open_descriptor_no_replace(\n            source_fd=source_fd,\n            root_fd=parent_fd,\n            target_name=witness_name,\n        )\n        linked = _descriptor_entry_stat(root_fd=parent_fd, name=witness_name)\n        if (\n            linked is None\n            or not stat.S_ISREG(linked.st_mode)\n            or _stat_descriptor_identity(linked) != _stat_descriptor_identity(source)\n        ):\n            raise MRL0801HfAcquisitionError(\n                "atomic publication witness produced an invalid identity"\n            )\n    except MRL0801HfAcquisitionError:\n        raise\n    except OSError:\n        raise MRL0801HfAcquisitionError(\n            "atomic descriptor publication capability witness failed safely"\n        ) from None\n    finally:\n        if source_fd is not None:\n            os.close(source_fd)\n        if parent_fd is not None:\n            os.close(parent_fd)\n''',
)

replace(
    "src/medscale/mesc/_mrl_0801_hf_acquisition_v1.py",
    '    required_dir_fd = (os.mkdir, os.open, os.rmdir, os.stat, os.unlink)\n',
    '    required_dir_fd = (os.open, os.stat)\n',
)

replace(
    "scripts/mesc_mrl_0801_hf_acquire.py",
    '''def _probe_receipt_atomic_publication(output: _BoundReceiptOutput) -> None:\n    """Prove receipt atomic publication on the bound filesystem before acquisition."""\n    _require_bound_output_parent_identity(output)\n    probe_dir_name = f".mrl-0801-receipt-probe-{secrets.token_hex(16)}"\n    probe_fd: int | None = None\n    source_fd: int | None = None\n    created_dir = False\n    published = False\n    try:\n        os.mkdir(probe_dir_name, 0o700, dir_fd=output.descriptor)\n        created_dir = True\n        probe_fd = os.open(\n            probe_dir_name,\n            os.O_RDONLY | _O_DIRECTORY | _O_NOFOLLOW | _O_CLOEXEC,\n            dir_fd=output.descriptor,\n        )\n        probe_directory = os.fstat(probe_fd)\n        if not stat.S_ISDIR(probe_directory.st_mode):\n            raise AcquisitionEntrypointError(\n                "atomic receipt publication probe did not bind a directory"\n            )\n        try:\n            source_fd = os.open(\n                ".",\n                os.O_WRONLY | _O_TMPFILE | _O_CLOEXEC,\n                0o600,\n                dir_fd=probe_fd,\n            )\n        except OSError:\n            raise AcquisitionEntrypointError(\n                "unnamed receipt publication is unsupported on this filesystem"\n            ) from None\n        source = os.fstat(source_fd)\n        if not stat.S_ISREG(source.st_mode):\n            raise AcquisitionEntrypointError(\n                "atomic receipt publication probe did not create a regular file"\n            )\n        probe_output = _BoundReceiptOutput(\n            path=output.parent_path / probe_dir_name / "probe",\n            parent_path=output.parent_path / probe_dir_name,\n            descriptor=probe_fd,\n            device=probe_directory.st_dev,\n            inode=probe_directory.st_ino,\n            name="probe",\n        )\n        _publish_open_descriptor_no_replace(source_fd=source_fd, output=probe_output)\n        published = True\n        linked = _descriptor_output_stat(probe_output)\n        if (\n            linked is None\n            or not stat.S_ISREG(linked.st_mode)\n            or _stat_identity(linked) != _stat_identity(source)\n        ):\n            raise AcquisitionEntrypointError(\n                "atomic receipt publication probe produced an invalid identity"\n            )\n        os.unlink("probe", dir_fd=probe_fd)\n        published = False\n        os.close(source_fd)\n        source_fd = None\n        os.close(probe_fd)\n        probe_fd = None\n        os.rmdir(probe_dir_name, dir_fd=output.descriptor)\n        created_dir = False\n        _require_bound_output_parent_identity(output)\n    except AcquisitionEntrypointError:\n        raise\n    except OSError:\n        raise AcquisitionEntrypointError(\n            "atomic receipt publication capability probe failed safely"\n        ) from None\n    finally:\n        if source_fd is not None:\n            with contextlib.suppress(OSError):\n                os.close(source_fd)\n        if probe_fd is not None:\n            if published:\n                with contextlib.suppress(OSError):\n                    os.unlink("probe", dir_fd=probe_fd)\n            with contextlib.suppress(OSError):\n                os.close(probe_fd)\n        if created_dir:\n            with contextlib.suppress(OSError):\n                os.rmdir(probe_dir_name, dir_fd=output.descriptor)\n''',
    '''def _probe_receipt_atomic_publication(output: _BoundReceiptOutput) -> None:\n    """Prove receipt publication on the same filesystem without deleting probe namespace."""\n    _require_bound_output_parent_identity(output)\n    witness_parent_fd: int | None = None\n    source_fd: int | None = None\n    try:\n        witness_parent_fd = os.open(\n            "..",\n            os.O_RDONLY | _O_DIRECTORY | _O_NOFOLLOW | _O_CLOEXEC,\n            dir_fd=output.descriptor,\n        )\n        output_parent = os.fstat(output.descriptor)\n        witness_parent = os.fstat(witness_parent_fd)\n        if not stat.S_ISDIR(witness_parent.st_mode):\n            raise AcquisitionEntrypointError(\n                "atomic receipt publication witness parent is not a directory"\n            )\n        if witness_parent.st_dev != output_parent.st_dev:\n            raise AcquisitionEntrypointError(\n                "safe same-filesystem receipt publication witness is unavailable"\n            )\n        try:\n            source_fd = os.open(\n                ".",\n                os.O_WRONLY | _O_TMPFILE | _O_CLOEXEC,\n                0o600,\n                dir_fd=witness_parent_fd,\n            )\n        except OSError:\n            raise AcquisitionEntrypointError(\n                "unnamed receipt publication is unsupported on this filesystem"\n            ) from None\n        source = os.fstat(source_fd)\n        if not stat.S_ISREG(source.st_mode):\n            raise AcquisitionEntrypointError(\n                "atomic receipt publication witness did not create a regular file"\n            )\n        witness_name = f".mrl-0801-receipt-witness-{secrets.token_hex(16)}"\n        witness_output = _BoundReceiptOutput(\n            path=output.parent_path.parent / witness_name,\n            parent_path=output.parent_path.parent,\n            descriptor=witness_parent_fd,\n            device=witness_parent.st_dev,\n            inode=witness_parent.st_ino,\n            name=witness_name,\n        )\n        _publish_open_descriptor_no_replace(source_fd=source_fd, output=witness_output)\n        linked = _descriptor_output_stat(witness_output)\n        if (\n            linked is None\n            or not stat.S_ISREG(linked.st_mode)\n            or _stat_identity(linked) != _stat_identity(source)\n        ):\n            raise AcquisitionEntrypointError(\n                "atomic receipt publication witness produced an invalid identity"\n            )\n        _require_bound_output_parent_identity(output)\n    except AcquisitionEntrypointError:\n        raise\n    except OSError:\n        raise AcquisitionEntrypointError(\n            "atomic receipt publication capability witness failed safely"\n        ) from None\n    finally:\n        if source_fd is not None:\n            os.close(source_fd)\n        if witness_parent_fd is not None:\n            os.close(witness_parent_fd)\n''',
)

replace(
    "scripts/mesc_mrl_0801_hf_acquire.py",
    '    required_dir_fd = (os.mkdir, os.open, os.rmdir, os.stat, os.unlink)\n',
    '    required_dir_fd = (os.open, os.stat)\n',
)

replace(
    "specs/mesc-experiment-0/mrl-0801-hf-acquisition-provenance-v1.md",
    'A successful `O_TMPFILE` open or an exported `linkat` symbol alone is insufficient; the capability probe must successfully publish an unnamed file with `linkat(..., AT_EMPTY_PATH)`, verify the linked identity, and restore its isolated probe directory.',
    'A successful `O_TMPFILE` open or an exported `linkat` symbol alone is insufficient. The capability check must successfully publish an unnamed file with `linkat(..., AT_EMPTY_PATH)` into the descriptor-opened parent on the same device and verify the linked identity. The zero-byte hidden capability witness is intentionally retained; automatic witness unlink or directory removal is prohibited because namespace deletion cannot be bound atomically to a previously observed inode.',
)
replace(
    "specs/mesc-experiment-0/mrl-0801-hf-acquisition-provenance-v1.md",
    'Before any remote metadata request, it performs an isolated same-filesystem capability probe through the bound destination descriptor that creates an unnamed `O_TMPFILE`, successfully publishes it with `linkat(..., AT_EMPTY_PATH)`, verifies the linked inode identity, and removes only the private probe namespace before continuing.',
    'Before any remote metadata request, it opens the destination\'s actual parent through descriptor-relative `..`, requires the parent to be on the same device, creates an unnamed `O_TMPFILE` there, publishes a random hidden zero-byte witness with `linkat(..., AT_EMPTY_PATH)`, verifies the linked inode identity, and retains that witness rather than performing a racy namespace deletion.',
)
replace(
    "specs/mesc-experiment-0/mrl-0801-hf-acquisition-provenance-v1.md",
    'Before acquisition begins, every bound receipt parent must pass the same successful same-filesystem `O_TMPFILE` plus `linkat(..., AT_EMPTY_PATH)` capability proof.',
    'Before acquisition begins, every bound receipt parent opens its actual parent through descriptor-relative `..`, requires the same device, and leaves an independently verified hidden zero-byte publication witness there. No capability-witness cleanup is automatic.',
)
