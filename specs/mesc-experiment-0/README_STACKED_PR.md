# Stacked Successor Review Flow

This package is intentionally prepared on top of PR #373. It may be reviewed early but must
not merge to `main` until #373 is canonical. After parent merge, retarget to exact canonical
`main`, refresh exact-head CI/CodeQL/review/thread qualification, and merge only through the
active ruleset.
